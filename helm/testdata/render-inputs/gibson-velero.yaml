# render-inputs/gibson-velero.yaml — what an installer supplies, for the offline guards.
# See render-inputs/gibson.yaml. The gibson-velero release has no default
# bucket either.
bucket:
  name: "example-durable-bucket"
